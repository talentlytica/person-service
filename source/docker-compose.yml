version: '3.8'

services:
  person-service:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_VERSION: ${APP_VERSION:-dev}
    container_name: person-service
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
